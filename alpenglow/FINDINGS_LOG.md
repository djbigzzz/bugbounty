# Alpenglow Audit — Findings Log

Cited commit: `f01d78db8a89c36bee025cda13413e1bdca1fb50` (master HEAD, 2026-08-06).
Status legend: 🔴 submit-candidate · 🟡 needs verification · ⚪ latent/non-exploitable · ✅ verified sound (negative result).

Nothing here is submission-ready until it has a **reproducing PoC** and passes the REPORT_TEMPLATE pre-flight checklist. We only burn 0.5 SOL on 🔴 items with a working PoC.

---

## Area 1 — Certificate & signature verification  (agent: cert/sigverify) — RETURNED, re-running on identity lead
- ⚪ **F1: `AggregateAccumulator::add_aggregate` adds stake unconditionally while OR-ing rank bits.** If overlapping ranks ever reached it, stake would double-count and a locally-built cert could cross threshold on inflated stake. Not remotely reachable today — every upstream path dedups (bls-sigverify `VotePool` rejects repeated `(rank,type,block)`; external aggregates attributed only to gossip sender's own rank). Receivers unaffected (they recompute stake from the bitmap). *Recommend a dedup guard; latent hazard, not a submission.* `votor/src/aggregate_accumulator.rs:57-67`
- ✅ Threshold math, epoch binding (build==verify use same lookup), domain separation (type tag + shred_version in every payload), `PopVerified::new_unchecked` chain, empty-set → error, reward-cert credited set = HashSet backed by verifying sig. All verified sound.
- 🟡 **OPEN (priority):** does the *verifier* reject an identity-signature certificate? The builder-side fix `71e6c74` landed mid-window; verifier parity is being checked now.

## Area 4 — Migration & wire format  (agent: migration) — RETURNED
- ⚪ **M-vs-G dual migration-slot definition.** `MigrationStatus` uses M = activation_slot + MIGRATION_SLOT_OFFSET(5000); `Bank::get_alpenglow_migration_slot` uses G = genesis_cert.block.slot (< M). They derive to different epochs only if M crosses an epoch boundary G does not. The 5000-slot offset is designed to keep migration away from boundaries, so on realistic params `get_epoch(G)==get_epoch(M)` and they coincide; even in the contrived straddle the behavioral delta is minor (legacy TowerBFT thread shutdown, not consensus). Sibling of known #14207 but a distinct location. *SPECULATIVE, likely non-exploitable.* `votor-messages/src/migration.rs:407,414-415`; `runtime/src/bank.rs:6701-6704`; `runtime/src/alpenglow_epoch_type.rs:156-159`
- ✅ Wire format re-derives the signed bytes from `(Vote, shred_version)` rather than trusting wire bytes → no malleability/forgery; unknown tags rejected; shred_version validated at decode and bound into every payload → no cross-cluster/fork replay.
- ✅ Genesis threshold 82% (`migration.rs:88`); two conflicting 82% genesis certs need 64% double-voters vs a 20% fault budget — infeasible. Cutover trigger deterministic (M derived from consensus-agreed activation slot). Genesis cert bound to `(slot,block_id)`, not replayable.
- ✅ Migration "cannot recover" panics all require an already-verified 82% genesis cert or internal error — not remote-reachable. Deserialization allocations bounded.
- ❗ Handoff: did **not** trace `block_component_processor/vote_reward.rs` lamport math end-to-end (loss-of-funds class) — flagged for the rewards agent.

## Area 3 — Votor event loop, timers, voting  (agent: event-loop) — RETURNED
- 🟡/⚪ **Non-durable `vote_history` persistence → self-equivocation after power loss.** `FileVoteHistoryStorage::store` writes `*.bin.new` + `fs::rename` with **no `sync_all()`** on file or parent dir (explicit perf comments, `vote_history_storage.rs:176-184`). Persist-vs-send ordering is otherwise correct (wire vote emitted strictly after `store()` returns Ok). But on **power loss / kernel panic** (not an ordinary process crash — page cache survives that) after the wire vote was delivered but before the rename is durable, restart can load the **previous, fully-valid** history missing the last vote; `restore` returns Ok so the fail-safe (`require_vote_history`) does NOT trigger, and the validator can then cast a conflicting vote for that slot → self-equivocation (safety). `voting_utils.rs:286-309`, `event_handler.rs:203-213`, `validator.rs:2055-2087`.
  - **Caveats that lower its value:** requires true power/kernel loss (probabilistic, not attacker-controlled remotely); the non-fsync choice is explicit and mirrors long-standing TowerBFT `SavedTower` → **novelty and Alpenglow-gating both questionable** (a reviewer may call it out-of-scope or an accepted trade-off). PoC needs a fault-injecting FS (dm-flakey / LD_PRELOAD dropping un-fsynced writes). **Decision: hold — verify it's not TowerBFT-identical and not already accepted before considering a burn.**
- ✅ **In-window fix `b29a479` (genesis skip) audited COMPLETE.** `initialize_genesis(genesis_slot)` is called at `event_handler.rs:149`, strictly before the event loop starts (line 166), on every path; unaligned-migration case (genesis_slot≠0) correctly keeps the genesis slot out of the skip range via `voted(g)==true`; genesis_slot derives from the agreed on-chain migration block, so no remote/partition vector. Bug not reintroduced.
- ✅ Timer arithmetic (checked_add/sub, delta clamped to MAX_TIMEOUT=1h, assert delta_first_fec_set≤delta_block always passed equal); standstill multiplier overflow unreachable (~11yr); heap/timer-map invariant maintained; skip-cascade has no un-rearmed-timer wedge; `add_vote` equivocation asserts all guarded by caller early-returns (no remote panic). `check_pending_blocks` O(n²) during stuck-root standstill is local CPU only, below DoS bar.

## Area 2 — Consensus pool & vote accounting  (agent: consensus-pool) — running
## Area 5 — Reward certs & validator admission  (agent: rewards) — RETURNED — SOLID (high confidence)
- ✅ **No unearned credit.** Replay path uses `ValidatedRewardCert::try_new` (not the leader's trusting `try_new_for_leader`); the credited `validators` set is populated only as a side effect of the rank-map closure inside `verify_base2`, so a bit set for a non-signer makes the aggregate signature mismatch and the whole cert rejects. Credited pubkey and stake both come from the same `epoch_stakes_from_slot(reward_slot)`.
- ✅ **No double credit.** `extract_slot` requires `reward_slot + NUM_SLOTS_FOR_REWARD(8) == current_slot` → a reward cert for slot R is only valid in the block at R+8; one rooted block per slot ⇒ credited once. Finalize-cert path pays zero lamports. Combined path visits each validator once (`union`).
- ✅ **Arithmetic conserves.** `reward = max_validator_reward * stake_i / (slots_per_epoch * total_stake)` in u128, telescopes to exactly `max_validator_reward`/epoch; no attacker-controlled input; every failure mode errs toward under-payment.
- ✅ **Strongest lead ruled out: rank-map dedup eviction.** `epoch_stakes.rs:111-123` drops both copies of a duplicated BLS/node pubkey (could shrink total_stake / evict a victim) — but registering a BLS pubkey needs a PoP signed over `"ALPENGLOW" || vote_account_pubkey` (`programs/vote/src/vote_state/mod.rs`), so an attacker can't forge a victim's key or reuse a captured PoP. Node-pubkey collision blocked by `verify_authorized_signer`. Closed.
- ✅ Rank map deterministic (sort by stake desc, bls-pubkey asc — unique tiebreak); reward-cert bitmap parsing bounded (≤64 KiB, `decode` rejects OOB ranks); single shared parser (no dual-parser divergence); VAT burn is self-inflicted (own admission ticket to incinerator), no cross-account theft.
- ❗ Two load-bearing claims a PoC *could* confirm-as-safe (not bugs): (a) honest replayers reject a leader block whose `try_new_for_leader` set disagrees with its emitted bitmap (bank-hash mismatch); (b) PoP binding prevents duplicate-BLS registration end-to-end. Both verified by source + in-tree adversarial tests.
## Area 6 — Network integration points  (agent: networking) — running
